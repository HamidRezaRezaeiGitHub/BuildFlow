Title:
[frontend] <short action> — <area/component>

Context:
<Current behavior/problem (1–2 lines).>
<Desired behavior in one line.>

Read before acting:
- Inspect or update these files: (No need to provide full paths)
  - [frontend] <File>.tsx
  - [frontend] src/pages/<Page>.tsx
  - [frontend] <Component>.tsx

Acceptance Criteria:
- <AC1: measurable behavior or result>
- <AC2: responsive design expectation (mobile vs. desktop)>
- <AC3: accessibility/usability goal>
- 'npm run test', 'npm run lint', and 'npm run build' complete without errors.
- Related README.md files are updated if necessary. 
- Update Files Structure of README.md at the root of the project, if new files/modules are added (or removed).

Constraints & Hints:
- <Constraint 1>
- <Constraint 2>
- <Hint 1>
- <Hint 2>

---
### 🧩 Example

Title:
[frontend] Fix DevPanel visibility — AppRouter integration

Context:
DevPanel is rendered via AppRouter but disappears on small screens. Visibility should depend on the config flag only, not viewport size.

Read before acting:
- [frontend] AppRouter.tsx
- [frontend] DevPanel.tsx

Acceptance Criteria:
- DevPanel visible when configuration is enabled across all breakpoints.
- No overlap with bottom navbar or FAB on mobile.
- 'npm run test', 'npm run lint', and 'npm run build' complete without errors.
- README.md in the frontend module is updated if necessary.
- Update Files Structure of README.md at the root of the project, if new files/modules are added (or removed).

Constraints & Hints:
- Likely hidden by responsive classes (`hidden sm:block`, etc.) or wrapper CSS.
- Keep changes localized—avoid refactoring unrelated components.