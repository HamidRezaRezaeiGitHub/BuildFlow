# Development Environment Configuration
# Used when running: npm run dev

# Application Mode
VITE_APP_MODE=standalone
# Options: 'standalone' | 'integrated'

# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api
# When standalone: uses mock/local API, When integrated: would use http://localhost:8080/api

# Backend Integration
VITE_BACKEND_ENABLED=false
# Set to true when running with Spring Boot backend

# Feature Flags
VITE_ENABLE_MOCK_AUTH=true
VITE_ENABLE_MOCK_DATA=true
VITE_ENABLE_CONSOLE_LOGS=true
# Mock features enabled for standalone demonstration

# Environment Name
VITE_ENVIRONMENT=development
