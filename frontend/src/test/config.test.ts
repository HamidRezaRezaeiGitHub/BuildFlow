/**
 * Simple test to verify Jest configuration is working
 */
describe('Jest Configuration', () => {
  test('jest_should_work_when_configured_properly', () => {
    expect(true).toBe(true);
  });
});